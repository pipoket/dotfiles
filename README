pipoket's used dotfiles.

To install use:
    curl https://github.com/pipoket/dotfiles/raw/master/install.sh > /tmp/dotfiles_installer.sh; bash /tmp/dotfiles_installer.sh; rm /tmp/dotfiles_installer.sh
